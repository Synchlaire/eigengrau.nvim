<DIRECTIONS>
can you check why is this giving me a warning? it keeps saying this requires an
argument. fix it
</DIRECTIONS>
<Context>
You receive a selection in neovim that you need to replace with new code.
The selection's contents may contain notes, incorporate the notes every time if there are some.
consider the context of the selection and what you are suppose to be implementing
<SELECTION_LOCATION>
range(point(47,1),point(52,19))
</SELECTION_LOCATION>
<SELECTION_CONTENT>
      _99.visual()
    end, { desc = "99: Visual AI" })

    -- Search across project with AI
    vim.keymap.set("n", "<leader>is", function()
      _99.search()
</SELECTION_CONTENT>
<FILE_CONTAINING_SELECTION>
return {
  "ThePrimeagen/99",
  lazy = true,
  keys = {
    { "<leader>iv", mode = "v", desc = "99: Visual AI" },
    { "<leader>is", mode = "n", desc = "99: Search" },
    { "<leader>ix", mode = "n", desc = "99: Stop all" },
    { "<leader>il", mode = "n", desc = "99: View logs" },
  },
  config = function()
    local _99 = require("99")

    _99.setup({
      -- Use opencode provider
      provider = _99.Providers.OpenCodeProvider,

      -- Enable debug logging to file
      logger = {
        level = _99.DEBUG,
        path = vim.fn.stdpath("log") .. "/99.log",
        print_on_error = true,
      },

      -- Temp directory for AI operations
      tmp_dir = "./tmp",

      -- Completions: #rules and @files in prompt buffer
      completion = {
        source = "blink", -- or "cmp" if using nvim-cmp
        -- Custom SKILL.md rules directories
        custom_rules = {},
        files = {
          enabled = true,
          max_file_size = 102400,
          max_files = 5000,
        },
      },

      -- Auto-load AGENT.md files from project directories
      md_files = {
        "AGENT.md",
      },
    })

    -- Visual selection: send to AI and replace with result
    vim.keymap.set("v", "<leader>iv", function()
      _99.visual()
    end, { desc = "99: Visual AI" })

    -- Search across project with AI
    vim.keymap.set("n", "<leader>is", function()
      _99.search()
    end, { desc = "99: Search" })

    -- Stop all in-flight requests
    vim.keymap.set("n", "<leader>ix", function()
      _99.stop_all_requests()
    end, { desc = "99: Stop all" })

    -- View logs
    vim.keymap.set("n", "<leader>il", function()
      _99.view_logs()
    end, { desc = "99: View logs" })
  end,
}
</FILE_CONTAINING_SELECTION>

</Context>

<Location><File>/home/claroscuro/.config/nvim/lua/eigengrau/plugins/tools/99.lua</File><Function>range(point(47,1),point(52,19))</Function></Location>
<FunctionText>      _99.visual()
    end, { desc = "99: Visual AI" })

    -- Search across project with AI
    vim.keymap.set("n", "<leader>is", function()
      _99.search()</FunctionText>
<MustObey>
NEVER alter any file other than TEMP_FILE.
never provide the requested changes as conversational output. Return only the code.
ONLY provide requested changes by writing the change to TEMP_FILE

never attempt to read TEMP_FILE.
It is purely for output.
Previous contents, which may not exist, can be written over without worry
After writing TEMP_FILE once you should be done.  Be done and end the session.

</MustObey>
<TEMP_FILE>./tmp/99-2062</TEMP_FILE>